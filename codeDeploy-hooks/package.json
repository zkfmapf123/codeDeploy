{
  "name": "codedeploy-hooks",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "clean": "rm -rf node_modules && rm -rf *.zip && npm install",
    "zip": "zip -r function.zip .",
    "upload": "npm run clean && npm run zip",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@slack/webhook": "^6.1.0",
    "aws-sdk": "^2.1430.0"
  },
  "devDependencies": {
    "@types/aws-sdk": "^2.7.0"
  }
}
